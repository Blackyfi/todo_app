name: todo_app
description: "A full-featured task management application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.3.2
  sqflite_common_ffi: ^2.3.2+1
  path: ^1.9.0
  intl: ^0.20.2
  flutter_local_notifications:
    path: ./packages/flutter_local_notifications-16.3.3
  desktop_notifications: ^0.6.3  # For desktop notifications
  timezone: ^0.9.2
  fl_chart: ^0.70.2
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  share_plus: ^10.1.4
  package_info_plus: ^8.3.0
  provider: ^6.1.1
  app_settings: ^5.1.1  # For opening app settings

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true