name: todo_app
description: "A full-featured task management application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.3.2
  sqflite_common_ffi: ^2.3.2+1
  path: ^1.9.0
  intl: ^0.20.2
  flutter_local_notifications: ^19.2.1
  #flutter_local_notifications:
    # path: ./packages/flutter_local_notifications-16.3.3
  desktop_notifications: ^0.6.3  # For desktop notifications
  timezone: ^0.10.0
  fl_chart: ^1.0.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  share_plus: ^11.0.0
  package_info_plus: ^8.3.0
  provider: ^6.1.1
  app_settings: ^6.1.1  # For opening app settings
  cross_file: ^0.3.4+2

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true